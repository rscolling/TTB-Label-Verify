[pytest]
testpaths = tests
addopts = -q
markers =
    live: hits the live Anthropic API (requires ANTHROPIC_API_KEY; skipped otherwise)
    e2e: browser-driven UI tests (skip cleanly when Playwright/Chromium is unavailable)
